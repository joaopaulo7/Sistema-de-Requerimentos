entrada = document.getElementById("departamento");
seletor = document.getElementById("infuncao");

contentselect.onchange = function(){
         var text = seletor.options[funcao.selectedIndex].value
         if(text != ""){
             entrada.value = text;
         }
    }

